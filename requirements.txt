flask>=3.0
astropy>=6.0
numpy>=1.24
# Required for astropy SkyCoord.match_to_catalog_sky (KDTree backend)
# used by /api/export/priority.
scipy>=1.10
# Optional — only needed for scripts/fetch_catalogs.py
astroquery>=0.4.7
# Required for MOC overlay support (data/surveys.json + /api/moc/<id>); ACP gracefully degrades if absent.
mocpy>=0.13
