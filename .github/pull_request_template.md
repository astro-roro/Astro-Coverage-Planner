## Summary

<!-- 1-3 bullets on what changed and why -->

## Smoke check

Uncheck items that don't apply. Skip the section entirely for doc/CI-only PRs.

- [ ] Page loads without console errors
- [ ] Aladin map renders + overlays toggle (catalogues, MOCs)
- [ ] Search bar filters the rail (try a bareword + a `key:value` like `tel:rasa`)
- [ ] Plan rail renders + an accordion opens
- [ ] Anything I changed visually looks right at the default window size
