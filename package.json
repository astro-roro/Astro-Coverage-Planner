{
  "name": "astro-coverage-planner-frontend-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Node built-in test runner for the static/ frontend modules. The web app itself is served by app.py and has no build step.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/frontend/"
  }
}
